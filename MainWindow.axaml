<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:WaterElectricityAutoClient.ViewModels"
        xmlns:local="using:WaterElectricityAutoClient"
        x:Class="WaterElectricityAutoClient.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="珠海科技学院宿舍电费查询与推送系统" Width="760" Height="540"
        MinWidth="580" MinHeight="420"
        Background="{DynamicResource ContentBrush}"
        Closing="OnWindowClosing">

    <Window.Styles>
        <Style Selector="ListBoxItem">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="4,1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ValueBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
        </Style>
        <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource NavHoverBrush}"/>
        </Style>
        <Style Selector="ListBoxItem:selected">
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TitleBrush}"/>
        </Style>
        <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource NavSelectedBrush}"/>
        </Style>
    </Window.Styles>

    <Grid ColumnDefinitions="Auto,*">
        <!-- Sidebar -->
        <Border Grid.Column="0"
                Width="{Binding SidebarWidth}"
                Background="{DynamicResource SidebarBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <Border.Transitions>
                <Transitions>
                    <DoubleTransition Property="Width" Duration="0:0:0.25" Easing="CubicEaseInOut"/>
                </Transitions>
            </Border.Transitions>
            <Grid RowDefinitions="48,*,Auto">
                <!-- Toggle button -->
                <Button Grid.Row="0"
                        Command="{Binding ToggleSidebarCommand}"
                        Background="Transparent"
                        Foreground="{DynamicResource TitleBrush}"
                        HorizontalAlignment="Stretch" VerticalContentAlignment="Center"
                        Content="&#x2630;" FontSize="20"
                        Height="48" Padding="0" Cursor="Hand">
                    <Button.Styles>
                        <Style Selector="Button:pointerover /template/ ContentPresenter">
                            <Setter Property="Background" Value="{DynamicResource SidebarHoverBrush}"/>
                        </Style>
                        <Style Selector="Button:pressed /template/ ContentPresenter">
                            <Setter Property="Background" Value="{DynamicResource SidebarPressedBrush}"/>
                        </Style>
                    </Button.Styles>
                </Button>

                <!-- Nav items -->
                <ListBox Grid.Row="1"
                         SelectedIndex="{Binding SelectedNavIndex}"
                         Margin="0,4"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBoxItem>
                        <Grid ColumnDefinitions="40,*" Height="40" VerticalAlignment="Center">
                            <TextBlock Grid.Column="0" Text="&#x1F50D;"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="查询"
                                       VerticalAlignment="Center" FontSize="13.5"
                                       Foreground="{DynamicResource ValueBrush}"
                                       Margin="4,0,0,0"
                                       Opacity="{Binding IsSidebarExpanded, Converter={x:Static local:AppConverters.BoolToDouble}}">
                                <TextBlock.Transitions>
                                    <Transitions>
                                        <DoubleTransition Property="Opacity" Duration="0:0:0.2" Easing="SineEaseInOut"/>
                                    </Transitions>
                                </TextBlock.Transitions>
                            </TextBlock>
                        </Grid>
                    </ListBoxItem>
                    <ListBoxItem>
                        <Grid ColumnDefinitions="40,*" Height="40" VerticalAlignment="Center">
                            <TextBlock Grid.Column="0" Text="&#x2699;"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="推送设置"
                                       VerticalAlignment="Center" FontSize="13.5"
                                       Foreground="{DynamicResource ValueBrush}"
                                       Margin="4,0,0,0"
                                       Opacity="{Binding IsSidebarExpanded, Converter={x:Static local:AppConverters.BoolToDouble}}">
                                <TextBlock.Transitions>
                                    <Transitions>
                                        <DoubleTransition Property="Opacity" Duration="0:0:0.2" Easing="SineEaseInOut"/>
                                    </Transitions>
                                </TextBlock.Transitions>
                            </TextBlock>
                        </Grid>
                    </ListBoxItem>
                </ListBox>

                <!-- Bottom area: theme toggle + version -->
                <StackPanel Grid.Row="2" Margin="4,4,4,8" Spacing="4">
                    <Button Command="{Binding ToggleThemeCommand}"
                            Background="Transparent"
                            Height="36" Padding="4,0" Cursor="Hand"
                            VerticalContentAlignment="Center"
                            HorizontalContentAlignment="Center">
                        <Button.Styles>
                            <Style Selector="Button:pointerover /template/ ContentPresenter">
                                <Setter Property="Background" Value="{DynamicResource SidebarHoverBrush}"/>
                            </Style>
                        </Button.Styles>
                        <Grid ColumnDefinitions="40,*">
                            <TextBlock Grid.Column="0" Text="&#x2600;"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       IsVisible="{Binding IsDarkTheme}"/>
                            <TextBlock Grid.Column="0" Text="&#x1F319;"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       IsVisible="{Binding IsDarkTheme, Converter={x:Static BoolConverters.Not}}"/>
                            <TextBlock Grid.Column="1" Text="深色模式"
                                       VerticalAlignment="Center" FontSize="13"
                                       Margin="4,0,0,0"
                                       Foreground="{DynamicResource LabelBrush}"
                                       Opacity="{Binding IsSidebarExpanded, Converter={x:Static local:AppConverters.BoolToDouble}}">
                                <TextBlock.Transitions>
                                    <Transitions>
                                        <DoubleTransition Property="Opacity" Duration="0:0:0.2" Easing="SineEaseInOut"/>
                                    </Transitions>
                                </TextBlock.Transitions>
                            </TextBlock>
                        </Grid>
                    </Button>
                    <TextBlock Text="水电查询 v1.1"
                               Foreground="{DynamicResource HintBrush}" FontSize="11"
                               Margin="12,0" IsVisible="{Binding IsSidebarExpanded}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content area -->
        <Border Grid.Column="1" Background="{DynamicResource ContentBrush}">
            <ContentControl Content="{Binding CurrentView}" />
        </Border>
    </Grid>
</Window>
